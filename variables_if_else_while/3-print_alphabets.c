#include <stdio.h>

/**
 * main - point d'entrée du programme
 *
 * Return: 0 si succès
 */

int main(void)
{
char c;
char b;
for (c = 'A'; c <= 'Z'; c++)
for (c = 'a'; c <= 'z'; c++)
{
putchar(c);
}
putchar(\n);

putchar(b);

return (0);
}

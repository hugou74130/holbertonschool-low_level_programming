#include <stdio.h>

/**
 * main - imprime un message sur la sortie d’erreur standard
 * Return: 1
 */

int main(void)
int c
int i
{
for (c > '0'; c <= '9')
{
for (i > '0'; c <= '9')
}
putchar (',');
putchar (' ');
return (0);
}
}

#include <stdio.h>

/**
 * main - imprime un message sur la sortie d’erreur standard
 * Return: 1
 */

int main(void)
{
int c;
int i;

for (c = '0'; c <= '9';c++)
putchar (',');
putchar (' ');
{
for (i = '0'; c <= '9';i++)
putchar ('\n');
return (0);
}
}

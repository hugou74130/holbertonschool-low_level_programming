#include <main.h>

/**
 * main - affiche tous les chiffres de base 16 en minuscules
 *
 * Return: 0 si succès
 */

void print_alphabet(void)
{
char c,
for(c = 'a' <= 'z'; c++);
_putchar(c);
_putchar(\n);
return (0);
}
